import 'package:flutter/material.dart';

class AddRoomRoutes extends StatelessWidget {
  const AddRoomRoutes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Add Room"),
    );
  }
}
