import 'package:flutter/material.dart';

class UpdateRoomRoutes extends StatelessWidget {
  const UpdateRoomRoutes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Update Room"),
    );
  }
}
