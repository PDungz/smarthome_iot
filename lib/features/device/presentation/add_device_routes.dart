import 'package:flutter/material.dart';

class AddDeviceRoutes extends StatelessWidget {
  const AddDeviceRoutes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Add Device"),
    );
  }
}
