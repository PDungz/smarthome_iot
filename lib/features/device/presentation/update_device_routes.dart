import 'package:flutter/material.dart';

class UpdateDeviceRoutes extends StatelessWidget {
  const UpdateDeviceRoutes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Update Device"),
    );
  }
}
